
  <div class="relative flex items-center space-x-3 rounded-lg border border-gray-300 bg-white px-6 py-5">
    <div class="min-w-0 flex-1">
      <%= link_to competition.name, edit_competition_path(competition), class: "text-md font-medium text-indigo-900" %>
      <br>
      <%= link_to "#{competition.fixtures.count} fixtures", competition_fixtures_path(competition), class: "truncate text-sm text-indigo-500" %>
    </div>
  </div>